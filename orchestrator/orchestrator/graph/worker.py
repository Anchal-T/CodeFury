"""Level 0 — Worker node: one ZCode subprocess in an isolated git worktree (plan §2)."""


def worker_node(state: dict) -> dict:
    """Run a single Task through zcode_runner in a worktree and emit a Report."""
    raise NotImplementedError("Phase 1")
